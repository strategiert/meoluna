version: '3.8'

services:
  paddleocr:
    build: .
    container_name: meoluna-paddleocr
    ports:
      - "8001:8000"
    environment:
      - TZ=Europe/Berlin
      - OCR_LANGUAGE=german
    restart: unless-stopped
    # Uncomment for GPU support (requires nvidia-docker):
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
